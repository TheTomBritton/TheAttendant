{
    "name": "maid-of-threads",
    "version": "1.0.0",
    "description": "Maid of Threads — frontend build tooling",
    "private": true,
    "scripts": {
        "dev": "npm run --prefix sites/maid-of-threads dev",
        "build": "npm run --prefix sites/maid-of-threads build",
        "preview": "npm run --prefix sites/maid-of-threads preview"
    },
    "devDependencies": {
        "autoprefixer": "^10.4",
        "postcss": "^8.4",
        "tailwindcss": "^3.4",
        "@tailwindcss/typography": "^0.5",
        "@tailwindcss/forms": "^0.5",
        "vite": "^6.0"
    },
    "dependencies": {
        "htmx.org": "^2.0"
    }
}
